{strip}
	<div>
		<p>{translate text="Already grouped holds will be moved to the new group" isPublicFacing=true}</p>
		<p>{translate text="Are you sure you want to group the selected holds?" isPublicFacing=true}</p>
	</div>
{/strip}