Setup
1. run database maintenance to add module and settings
2. enable the Aspen Progressive Web Application(PWA) module
3. add Aspen Progressive Web Application(PWA) settings

Settings:
for most firebase settings:
head to console.firebase.google.com
open your project
open the app within your project
General tab > under SDK setup and configuration click config 
for the vapid key:
cloud messaging > web configuration > web push certificates generate key pair or use the existing public key visible