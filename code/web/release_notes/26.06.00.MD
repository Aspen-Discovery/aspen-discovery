## Aspen Discovery Updates
### Administration Updates
- Add the ability to schedule offline mode in advance ([DIS-423](https://aspen-discovery.atlassian.net/browse/DIS-423)) (*KL*)

<div markdown="1" class="settings">

#### New Settings
- System Variables > Scheduled Offline Start
- System Variables > Scheduled Offline End
- System Variables > Scheduled Econtent Access

</div>

### API Updates
- In User API, added a check to grab the latest contact information from the ILS in getPatronProfile. ([DIS-2093](https://aspen-discovery.atlassian.net/browse/DIS-2093)) (*KK*, *MDN*)
- Add logPatronRequestExternal in Abstract API to include `debug` key if allowAppRequestLogging in API returns (only for LiDA requests) to force logging in Aspen LiDA. ([DIS-2100](https://aspen-discovery.atlassian.net/browse/DIS-2100)) (*KK*)
- Increase the length of method names that can be stored in the User App Request Log and trim data if necesary. ([DIS-2413](https://aspen-discovery.atlassian.net/browse/DIS-2413)) (*MDN*)
- adding checks for userID in placeHold since requests from LiDA were previously ignoring that value for linked account holds ([DIS-2553](https://aspen-discovery.atlassian.net/browse/DIS-2553)) (*IT*)
- If terms are not properly provided to getBulkTranslations return a proper response rather than an error. ([DIS-2512](https://aspen-discovery.atlassian.net/browse/DIS-2512)) (*MDN*)
- Properly return source for Aspen events within the searchLite API. ([DIS-2568](https://aspen-discovery.atlassian.net/browse/DIS-2568)) (*MDN*)
- Fix Hide All for Browse Categories with debugging on. ([DIS-2569](https://aspen-discovery.atlassian.net/browse/DIS-2569)) (*MDN*)

### Cron Updates
- Only keep the last 30 days of Cron Logs. ([DIS-2510](https://aspen-discovery.atlassian.net/browse/DIS-2510)) (*MDN*)

### eCommerce Updates
- Add display of an itemized receipt for Square payments. ([DIS-2384](https://aspen-discovery.atlassian.net/browse/DIS-2384)) (*SW*)
- Add E-Commerce Details Report for individual line items ([DIS-2394](https://aspen-discovery.atlassian.net/browse/DIS-2394)) (*SW*)

### Event Updates
#### Event Waiting Lists
##### Configuration
- If registration is enabled, administrators can enable a waiting list at the event level. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
- Administrators can decide on a set number of waiting list seats per event. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - The number of waiting list seats can be overridden for a specific date / time (a specific instance of an event).
- Administrators can configure how long an invitation to register from the waiting list remains valid (in hours). ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
##### User Experience
- Logged in patrons can join the waiting list of a full event. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - directly from the search results.
    - from an event's detail page.
    - or from their list of saved event instances.
- Patrons on a waiting list can see their position. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - directly from the search results.
    - from an event's detail page.
    - or from their list of saved event instances.
- Patrons can leave a waiting list. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - directly from the search results.
    - from an event's detail page.
    - or from their list of saved event instances.
- Waiting list seat availability is displayed. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - directly from the search results.
    - from an event's detail page.
    - or from their list of saved event instances.
- When a seat opens up (a registered patron unregisters, or an event's capacity is increased), the next patron on the waiting list is invited to complete their registration. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - the invited patron can register through the normal registration flow until their invitation expires.
    - if the invitation expires before the patron registers, the next patron on the waiting list is invited.
- This includes linked account support, which means: ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - patrons with linked users add and remove their linked users from the waiting list.
    - patrons with linked users can see whether their linked users are on a waiting list and, if so, in which position.
    - when a linked user cannot be reached for waiting list notifications, the primary account holder is contacted instead if they have a valid email and consented to email notifications for events.
#### Event Email Notifications
- Patrons can opt in to event email notifications from their account preferences. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
- Patrons who have opted in and have a contact email saved to their account receive an email when: ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
    - they are invited to register from the waiting list.
    - an event they are registered to is cancelled.
    - an instance of an event they are registered to is deleted.
- Patrons who have an open waiting list invitation see an actionable message on their MyAccount home page until they complete or decline their registration. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
- Patrons are informed when joining a waiting list whether their account is reachable for notifications, and are prompted to add an email and opt in if it is not. ([DIS-1909](https://aspen-discovery.atlassian.net/browse/DIS-1909)) (*CZ*)
#### Event Attendance Management
##### Configuration
- Staff registration of patrons for events and event attendance management are now controlled by separate permissions, allowing libraries to grant either capability independently. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- The existing "staff registration only" event setting now only restricts patron self-registration; it no longer blocks staff from registering patrons. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
##### Staff Attendance Management UI
- Staff with the appropriate permission can access a new Attendance Management page from the admin area. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*JOM & CZ*)
    - the page lists upcoming, current, and past registerable events that the staff member has location access to.
    - the list presented includes events for the past 12 and upcoming 12 months.
    - selecting an event shows the full registration list for that instance, with patron details (name, barcode, email) and registration metadata (when they registered, whether they attended).
- Staff can mark each registered patron as attended (or not) directly from the registration list. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*JOM & CZ*)
- Staff with the appropriate permission can register a patron for an event on their behalf. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*JOM & CZ*)
    - only events that are currently registerable are listed in the staff registration panel.
- Staff registration goes through the same flow as patron self-registration, so capacity and waiting list rules are respected (including waiting-list-aware capacity checks). ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- The registration list identifies which registrations were made by staff and which staff member performed them. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- Patrons registered by staff have the event added to their Saved Events list, flagged as having been saved by staff. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
    - staff are informed that a Saved Events entry will be created when they register a patron.
    - the entry is displayed in the patron's Saved Events as staff-saved.
- Cancelled registrations are no longer displayed in the patron's registration list. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
###### Event Registration List and CSV Downloads
- From the Attendance Management page for a specific event instance, staff can download the registration list for an event instance in either of two formats. ([DIS-1885](https://aspen-discovery.atlassian.net/browse/DIS-1885)) (*CZ*)
    - a plain-text list suitable for printing, with a checkbox per registration so attendance can be marked off by hand.
    - a CSV intended for spreadsheet use or further processing.
- Both downloads identify the event (title, date, time, library location) and include patron details (name, ILS barcode, email, post code, date of birth) and registration metadata (status, whether the registration was made by the patron or by a staff member — and which staff member — and the date registered). ([DIS-1885](https://aspen-discovery.atlassian.net/browse/DIS-1885)) (*CZ*)
    - the plain-text list shows the event details once in a header block above the rows; the CSV repeats them on every row so the file can be filtered or joined without losing context.
- The CSV additionally includes the event type, the recorded attendance state (Yes/No), the patron's submitted values for each custom registration field configured on the event type, and the attendee count submitted per attendee category assigned to the event type. ([DIS-1885](https://aspen-discovery.atlassian.net/browse/DIS-1885)) (*CZ*)

#### Custom Event Registration Fields
##### Configuration
- Each custom field is configured with an intended use: ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
    - "Event description section" — the existing staff-managed fields whose values are displayed publicly on the event page.
    - "Event registration form" — newly introduced patron-facing fields that appear on the registration form.
    - when building field sets, the field options available when building a field set are filtered to those whose use matches the set's use.
- Each event type is assigned one event information field set (retained existing behaviour) and one event registration field set (new field set type). ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
    - only field sets matching the relevant use are offered for each assignment.
##### Custom Registration Fields
- The fields from the registration field set assigned to an event's type are rendered on that event's registration form for patrons to fill in. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- Patrons' submitted values are persisted against their registration. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- Once a patron is registered, their submitted custom field values are displayed read-only on the registration view, both on the event page and from their list of saved event instances. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- The custom registration fields are only displayed when registration is currently available, or when the patron is already registered (so they can review their submitted values). ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
#### Multi-Attendee Event Registrations
##### Configuration
- Administrators can view/create/edit/delete event attendee categories (e.g. "Adults", "Children", "Carers") from a new admin page, accessible from the event administration section. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- Each event type can be assigned any number of attendee categories. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
    - per assignment, administrators can set a minimum and maximum count, and decide whether the category is required on the registration form.
    - each category can only be assigned once to a given type. Duplicates will not be saved.
##### User Experience
- When registering for an event whose type has attendee categories assigned, patrons enter a count for one or more category on the registration form. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
    - the category-specific min/max constraints configured for the event type are enforced on submission.
    - a tooltip clarifies that the patron should include themselves in the counts when relevant.
    - the system handles missing category information, treating the registration as a single seat.
    - events with no attendee category assigned are supported and also treat each registration as taking up a single seat.
- The category breakdown a patron submitted is displayed read-only on their registration view, both on the event page and from their list of saved event instances. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- Event capacity now reflects the total projected attendees across all registrations, not the number of registration records. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
    - available seats displayed on search results, event detail pages, and saved event instances all account for attendee counts.
    - registration is blocked when the requested attendee counts would exceed the remaining seats.
    - attendee categories are not used for waiting lists and do not impact waiting list behaviour.
##### Staff Experience
- When a staff member registers a patron for an event, the same attendee category inputs are presented in the staff registration modal, with the same validation rules. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- The Attendance Management page displays the attendee category breakdown for the event instance (totals per category), as well as the per-registration breakdown for each patron. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
- When a registration is cancelled, its attendee count entries are removed so the event's projected attendance stays accurate. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)
#### Staff Notes on Events
- Administrators can record internal notes directly on an event's edit form. The notes live on the event's settings page and are not surfaced anywhere else. ([DIS-1597](https://aspen-discovery.atlassian.net/browse/DIS-1597)) (*CZ*)

### Events updates (LocalHop)
- Add integration with LocalHop events ([DIS-2391](https://aspen-discovery.atlassian.net/browse/DIS-2391)) (*KL*)

<div markdown="1" class="settings">

#### New Permissions
- Events: Administer LocalHop Settings

#### New Settings
- Events > LocalHop - Settings

</div>

### Grouped Work Display
- Add a new option to show the earliest publication date in search results and full record view. ([DIS-2452](https://aspen-discovery.atlassian.net/browse/DIS-2452)) (*MDN*)
- When displaying Grouped Works from Browse Category, properly hide formats that should not show by default. ([DIS-2402](https://aspen-discovery.atlassian.net/browse/DIS-2402)) (*MDN*)
- Properly translate formats in the horizontal display. ([DIS-2482](https://aspen-discovery.atlassian.net/browse/DIS-2482)) (*MDN*)
- Update the display of audiobook duration so that it shows for both vertical and horizontal format display. ([DIS-2381](https://aspen-discovery.atlassian.net/browse/DIS-2381)) (*KL*)
- Add a setting for scoping the 'More Like This' feature. ([DIS-2403](https://aspen-discovery.atlassian.net/browse/DIS-2403)) (*KL*)

<div markdown="1" class="settings">

#### New Settings
- Catalog / Grouped Works > Grouped Work Display Settings > Full Record Display > Show Earliest Publication Date for Full Record
- Catalog / Grouped Works > Grouped Work Display Settings > Search Results > Show Earliest Publication Date in Search Results
- Primary Configuration > More Like This Settings

</div>

### Hold Updates
- Allow the Hold form to be bypassed when Prompt for Edition is enabled. ([DIS-2475](https://aspen-discovery.atlassian.net/browse/DIS-2475)) (*MDN*)
- Updates for holds on Non-Fiction materials with multiple available editions. ([DIS-2386](https://aspen-discovery.atlassian.net/browse/DIS-2386)) (*MDN*)
  - Sort editions by availability before sorting by publication date, so available editions are shown first. 
  - When placing a hold, if the selected edition is not the newest edition, force the user to confirm they want a hold on the older edition.
- Fix Go to Holds button not showing when bypassing the hold prompts. ([DIS-2368](https://aspen-discovery.atlassian.net/browse/DIS-2368)) (*SW*)
- Fix malformed HTML in the hyperhold modal button so the place hold confirmation renders correctly when a patron already has the title on hold. ([DIS-2474](https://aspen-discovery.atlassian.net/browse/DIS-2474)) (*CZ*)
- Ensure the standard place hold button is shown instead of the hyperhold button on systems that do not have hyperholds enabled. ([DIS-2474](https://aspen-discovery.atlassian.net/browse/DIS-2474)) (*CZ*)
- Hyperholds now display correctly for patrons with linked accounts. ([DIS-2653](https://aspen-discovery.atlassian.net/browse/DIS-2653)) (*CZ*)
- Hyperholds functionality (grouping) is now available also when all holds are listed, but only if only compatible holds are selected. ([DIS-2561](https://aspen-discovery.atlassian.net/browse/DIS-2561)) (*CZ*)

### Hoopla Updates
- Use `artistFormal` field over `artist` field to make author names more consistent with ILS records when displaying records. ([DIS-2385](https://aspen-discovery.atlassian.net/browse/DIS-2385)) (*KK*)
- Prefer the 'artistFormal' field over the 'artist' field when grouping and indexing hoopla records. ([DIS-2385](https://aspen-discovery.atlassian.net/browse/DIS-2385)) (*MDN*)
- Content Rating facets now include ratings from Hoopla titles. ([DIS-2478](https://aspen-discovery.atlassian.net/browse/DIS-2478)) (*YL*)
- Display the number of Hoopla Flex titles for each library in Hoopla Settings for version 2. ([DIS-2479](https://aspen-discovery.atlassian.net/browse/DIS-2479)) (*YL*)
- Add `SubTitleLanguage` and `DubLanguage` fields to the translations facet ([DIS-2461](https://aspen-discovery.atlassian.net/browse/DIS-2461)) (*KL*)

<div markdown="1" class="settings">

#### New Settings
- Hoopla Settings > Hoopla Library Information > Flex Title Count

</div>

### Indexing Updates
- Allow AR Facet data to be populated from MARC data. ([DIS-2392](https://aspen-discovery.atlassian.net/browse/DIS-2392)) (*MJ*)
- Add feature to hide MARC fields which have a privacy indicator set (541, 542, 561, 583) ([DIS-1145](https://aspen-discovery.atlassian.net/browse/DIS-1145)) (*JW*)
- Update arPoints in database to be stored as decimal values instead of integer values ([DIS-2099](https://aspen-discovery.atlassian.net/browse/DIS-2099)) (*KL*)
- When indexing EAN/UPC from 024 do not check indicator ([DIS-2201](https://aspen-discovery.atlassian.net/browse/DIS-2201)) (*KL*)
- Prevent indexing stalls and timeouts, by updating auto commit timeouts in SolrConfig, updating number of threads, and records per batch. ([DIS-2511](https://aspen-discovery.atlassian.net/browse/DIS-2511)) (*MDN*)

<div markdown="1" class="settings">

#### New Settings
- System Administration > System Variables > Indexing Settings > Solr Thread Count
- System Administration > System Variables > Indexing Settings > Solr Queue Size

</div>


### Koha Updates
- Add option to force new Aspen users to opt-in to Reading History regardless of Koha privacy settings ([DIS-1958](https://aspen-discovery.atlassian.net/browse/DIS-1958)) (*KK*)

<div markdown="1" class="settings">

#### New Settings
- Primary Configuration > Library Systems > Force patrons to opt-in to Reading History

</div>

### Language Updates
- Added a "Default Language" option to the Languages admin page, allowing administrators to designate any language as the system default for unauthenticated users, replacing the hardcoded English fallback. ([DIS-2411](https://aspen-discovery.atlassian.net/browse/DIS-2411)) (*LM*)
- On first visit, the interface now automatically detects the user's browser language preference (via the `Accept-Language` header) and applies it if the language is available in the catalog. ([DIS-2411](https://aspen-discovery.atlassian.net/browse/DIS-2411)) (*LM*)
- Language selection is now persisted in a long-lived cookie, preserving the user's preference across browser sessions without requiring a login. ([DIS-2411](https://aspen-discovery.atlassian.net/browse/DIS-2411)) (*LM*)

### Local ILL 
- Handle cases when Local ILL was not determining if a volume hold should be placed. ([DIS-2255](https://aspen-discovery.atlassian.net/browse/DIS-2255)) (*MDN*)

### Material Requests Updates
- Fix a division by zero error when viewing all filtered Material Requests with no matching results. ([DIS-2366](https://aspen-discovery.atlassian.net/browse/DIS-2366)) (*YL*)

### My Account Updates
- Add horizontal swiping filter bar at the top of Holds page to allow filtering by account, status, and/or format. ([DIS-2389](https://aspen-discovery.atlassian.net/browse/DIS-2389)) (*KK*, *MDN*)

### OverDrive Updates
- Add OverDrive Advantage ID support for the upcoming OverDrive collection token rotation.([DIS-2467](https://aspen-discovery.atlassian.net/browse/DIS-2467)) (*YL*)
    - Backfill Advantage IDs from stored tokens during indexing before rotation starts.
    - Use OverDrive Advantage ID as the primary identifier for Advantage collection matching.
    - Display Advantage IDs in the OverDrive API Information page.
- Add support for a secondary OverDrive Advantage collection per library. ([DIS-1926](https://aspen-discovery.atlassian.net/browse/DIS-1926)) (*YL*)
    - Allow a library to configure an additional Advantage collection alongside its primary Advantage collection.
    - Index titles that exist only in the additional Advantage collection.
    - Use the primary Advantage collection for availability when a title exists in both primary and additional collections.
    - Use the additional Advantage collection for availability only when the title is not in the primary collection.

<div markdown="1" class="settings">

#### New Settings
- OverDrive Settings > Authentication and Advantage Information > Overdrive Advantage Products ID
- OverDrive Settings > Authentication and Advantage Information > Overdrive Additional Advantage Products ID
- OverDrive Settings > Authentication and Advantage Information > Overdrive Additional Advantage Products Key

</div>

### Palace Project Updates
- Fix an error determining if a patron requires a PIN when connecting to Palace Project. ([DIS-2580](https://aspen-discovery.atlassian.net/browse/DIS-MDN)) (*MDN*)

### Polaris Updates
- Updated Fines page for Polaris to show credits in addition to fines. ([DIS-2395](https://aspen-discovery.atlassian.net/browse/DIS-2395)) (*MJ*)
- Allow updating libraries and locations to be disabled in Polaris exporter. ([DIS-2450](https://aspen-discovery.atlassian.net/browse/DIS-2450)) (*MJ*)

<div markdown="1" class="settings">

#### New Settings
- ILS Integration > Indexing Profiles > Polaris Settings > Load Libraries and Locations from the ILS

</div>

### Reading History Updates
- When searching reading history, search using solr rather than the database for improved relevancy. ([DIS-2451](https://aspen-discovery.atlassian.net/browse/DIS-2451)) (*MDN*)
  - Title, Author, and Series fields will be searched. 
  - Titles that no longer appear in the catalog will be hidden when searching.

### Saved Search Updates
- Allow users to choose which saved searches to send notifications for. ([DIS-2469](https://aspen-discovery.atlassian.net/browse/DIS-2469)) (*SW*)
- Fix issues with saved search email templates. ([DIS-2466](https://aspen-discovery.atlassian.net/browse/DIS-2466)) (*SW*)

### Series Updates
- Add a new version for series indexing that generates a series group based on title, author, and language ([DIS-2396](https://aspen-discovery.atlassian.net/browse/DIS-2396)) (*KL*, *MDN*)
- Show all active series in Administer Series page even if they have no members. ([DIS-2396](https://aspen-discovery.atlassian.net/browse/DIS-2396)) (*MDN*)
- Show grouped work series title when viewing series for debugging purposes. ([DIS-2396](https://aspen-discovery.atlassian.net/browse/DIS-2396)) (*MDN*)
- Search results now display series information from Grouped Work Display Info when indexed series and NoveList series are not available.([DIS-2412](https://aspen-discovery.atlassian.net/browse/DIS-2412)) (*YL*)
- Optionally allow 490 with a first indicator of 0 (or blank) to generate series records. ([DIS-2436](https://aspen-discovery.atlassian.net/browse/DIS-2436)) (*MDN*)

<div markdown="1" class="settings">

#### New Settings
- Series Indexing > version
- Series - Permanent Series Id (only updated via indexing)
- Series - Series Language (only updated via indexing)

</div>

### Sideload Updates
- Add error visibility to the 'Replace existing files?' operation in SideLoad uploads ([DIS-2367](https://aspen-discovery.atlassian.net/browse/DIS-2367)) (*LM*)

### Sierra Updates
- Add a library setting to control whether ILL items are renewable. ([DIS-2465](https://aspen-discovery.atlassian.net/browse/DIS-2465)) (*YL*)
- Allow Sierra ILL checkouts to renew without recordId. ([DIS-2420](https://aspen-discovery.atlassian.net/browse/DIS-2420)) (*YL*)

<div markdown="1" class="settings">

#### New Settings
- Library Systems > Interlibrary loans > Allow ILL Renewals

</div>

### System Administration Updates
- Add support for OAuth2 and OpenID Connect clients for API authentication and authorization. ([DIS-2214](https://aspen-discovery.atlassian.net/browse/DIS-2214)) (*KK*, *MDN*)

<div markdown="1" class="settings">

#### New Settings
- System Administration > OAuth2 Clients
- System Administration > OAuth2 Rate Limits
- System Administration > OpenID Connect

</div>

### System Message Updates
- Allow System Messages to be targeted to additional locations. ([DIS-2453](https://aspen-discovery.atlassian.net/browse/DIS-2453)) (*MDN*)
  - Can be targeted to Events, Your Searches, Preferences, Libby Options, Hoopla Options, Your Lists, and the Home Page
  - System Messages display under the header so they can be used as instructional text. 

### Two-Factor Authentication Updates
- Add option to enable Time-Based One-Time Password (TOPT) as a 2FA option for using authenticator apps like Google Authenticator, Authy, etc. ([DIS-2454](https://aspen-discovery.atlassian.net/browse/DIS-2454)) (*KK*) (*MDN*)

<div markdown="1" class="settings">

#### New Settings
- Primary Configuration > Two-factor Authentication > 2FA Method
- Primary Configuration > Two-factor Authentication > TOTP Issuer Name

</div>

### Web Builder Updates
- Fix loading pages with only a single component in the URL. ([DIS-2523](https://aspen-discovery.atlassian.net/browse/DIS-2523)) (*MDN*)

### Other Updates
- Allow testing different line terminators and locations in SIP Tester. ([DIS-2428](https://aspen-discovery.atlassian.net/browse/DIS-2428)) (*MDN*)
- Correct permissions within Location Settings. ([DIS-2430](https://aspen-discovery.atlassian.net/browse/DIS-2430)) (*MDN*)
- Respect readOnly and required flags for currency fields. ([DIS-2476](https://aspen-discovery.atlassian.net/browse/DIS-2476)) (*MDN*)
- If logins are restricted to the patron's home library, and they try to log into the incorrect library, add to the existing message the patron's home library with a link to the correct catalog. ([DIS-2401](https://aspen-discovery.atlassian.net/browse/DIS-2401)) (*KK*)
- When Grouped Words Display is set to horizontal ILS records pages will now reflect a similar design for location and format information. ([DIS-2388](https://aspen-discovery.atlassian.net/browse/DIS-2388)) (*KK*)
- Store original cover URLs by size and allow cover URLs to regenerate on reload. ([DIS-2286](https://aspen-discovery.atlassian.net/browse/DIS-2286)) (*YL*)
- Fix an issue where the Where Is It? modal could display incorrect item due dates when item due dates use YYYYMMDD format. ([DIS-2464](https://aspen-discovery.atlassian.net/browse/DIS-2464)) (*YL*)
- Bugfix for navigation arrows missing from "More Like This' display ([DIS-2378](https://aspen-discovery.atlassian.net/browse/DIS-2378)) (*JW*)
- Bugfix for searching patrons on ils_username instead of displayName in Community Engagement's Admin View ([DIS-2406](https://aspen-discovery.atlassian.net/browse/DIS-2406)) (*PA*)
- Let update script check if db is remote. ([DIS-2422](https://aspen-discovery.atlassian.net/browse/DIS-2422)) (*BNJ*)
- Improve performance of DatabaseCleanup cron by rewriting NOT IN subqueries as LEFT JOIN anti-joins. ([DIS-2126](https://aspen-discovery.atlassian.net/browse/DIS-2126)) (*KMH*)
- Fix 404 with working page for Grapes editors ([DIS-185](https://aspen-discovery.atlassian.net/browse/DIS-185)) (*OR*)
- Allow SMTP Relays that do not require authentication to be used. ([DIS-2492](https://aspen-discovery.atlassian.net/browse/DIS-2492)) (*MDN*)
- Fix error loading available database updates when plugins are disabled. ([DIS-2493](https://aspen-discovery.atlassian.net/browse/DIS-2493)) (*MDN*)
- Support filtering on 'date' fields ([DIS-2161](https://aspen-discovery.atlassian.net/browse/DIS-2161)) (*OR*)
- Disable unsupported sorts/filters for Events ([DIS-2161](https://aspen-discovery.atlassian.net/browse/DIS-2161)) (*OR*, *CZ*)
- Display file upload errors on save ([DIS-2516](https://aspen-discovery.atlassian.net/browse/DIS-2516)) (*CZ*)
- Improve handling of list sorting in MyAccount. ([DIS-2517](https://aspen-discovery.atlassian.net/browse/DIS-2517)) (*OR*)

## This release includes code contributions from
### ByWater Solutions
- Yanjun Li (YL)
- Imani Thomas (IT)
- Jonah Wilson (JW)
- Kyle Hall (KMH)

### Grove For Libraries
- Mark Noble (MDN)
- Kirstien Kroeger (KK)
- Mark Jarrell (MJ)
- Kodi Lein (KL)
- Stephen Wicks (SW)

### Nashville Public Libraries  
- Bryan Neil Jones (BNJ)  

### Open Fifth
- Chloe Zermatten (CZ)
- Jacob O'Mara (JOM)
- Pedro Amorim (PA)
- Olivia Reynolds (OR)

### Prairie Lakes Library System
- Jim Novy (JN) 

### Theke Solutions
- Lucas Montoya (LM)

## Special Testing thanks to
- Myranda Fuentes
- Mark Noble
- Sarah M St. Martin
- Aude Charillon
- Brad Smith
- Lizzie Todd
- Sierra Chastain-Rittue
- Tara Wood
- Alex Vancina
- Bryan Jones
- Cynthia Romanowski
- David Noe
- Elaine Greenfield
- James Staub
- Jonathan Newton
- Laurlei Kolvitz
- Olivia Reynolds
- Tricia Andrews

## Special Documentation thanks to
- Myranda Fuentes
- Sarah M St. Martin
- Melissa Brenneman
- Aude Charillon
- Imani Thomas
- Jordan Fields
- Linda Kramer
- Mark Noble
- Sierra Chastain-Rittue
- Tara Wood

## This release includes sponsored developments from
- CLEVNET Library Cooperation
- CW MARS
- Greater Manchester Inter Library Consortial System (GMILCS)
- Helen Plum Public Library
- Hoopla
- Lynx Library Consortium
- MAIN Library Alliance
- Metropolitan Library System
- Nashville Public Library
- Orange County Library System
- Prairie Lakes Library System
- SHARE
- SWAN Library Network
- Wyoming State Library (WYLD)
- Sefton Libraries, with funding through LibraryOn 


